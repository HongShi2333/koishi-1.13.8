/// <reference types="node" />
/// <reference types="ws" />
import { Adapter, Context, Schema } from '@satorijs/satori';
import { QQBot } from './bot';
export declare class WsClient<C extends Context = Context> extends Adapter.WsClient<C, QQBot<C>> {
    _sessionId: string;
    _s: number;
    _ping: NodeJS.Timeout;
    prepare(): Promise<import("ws").WebSocket>;
    heartbeat(): void;
    accept(): Promise<void>;
}
export declare namespace WsClient {
    interface Config extends Adapter.WsClientConfig {
    }
    const Config: Schema<Config>;
}
