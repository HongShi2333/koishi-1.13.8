import { Bot, Context, Quester, Universal } from '@satorijs/satori';
export declare function transformKey(source: any, callback: (key: string) => string): any;
export declare class SatoriBot<C extends Context = Context> extends Bot<C, Universal.Login> {
    http: Quester;
    internal: any;
    constructor(ctx: C, config: Universal.Login);
}
