import { Adapter, Context, Schema } from '@satorijs/satori';
import { TelegramBot } from './bot';
export declare class HttpServer<C extends Context = Context> extends Adapter<C, TelegramBot<C>> {
    static inject: string[];
    connect(bot: TelegramBot<C, TelegramBot.BaseConfig & HttpServer.Config>): Promise<void>;
}
export declare namespace HttpServer {
    interface Config {
        protocol: 'server';
        path?: string;
        selfUrl?: string;
    }
    const Config: Schema<Config>;
}
