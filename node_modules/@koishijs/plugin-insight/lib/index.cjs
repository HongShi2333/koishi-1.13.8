var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// external/webui/plugins/insight/src/index.ts
var src_exports = {};
__export(src_exports, {
  default: () => src_default
});
module.exports = __toCommonJS(src_exports);
var import_koishi = require("koishi");
var import_console = require("@koishijs/console");
var import_path = require("path");
var import_meta = {};
function format(name) {
  return (0, import_koishi.capitalize)((0, import_koishi.camelize)(name));
}
__name(format, "format");
function getName(plugin) {
  if (!plugin)
    return "App";
  if (!plugin.name || plugin.name === "apply")
    return "Anonymous";
  return format(plugin.name);
}
__name(getName, "getName");
function getSourceId(child) {
  const { state } = child.parent;
  if (state.runtime.isForkable) {
    return state.uid;
  } else {
    return state.runtime.uid;
  }
}
__name(getSourceId, "getSourceId");
var _Insight = class _Insight extends import_console.DataService {
  constructor(ctx) {
    super(ctx, "insight");
    ctx.console.addEntry(process.env.KOISHI_BASE ? [
      process.env.KOISHI_BASE + "/dist/index.js",
      process.env.KOISHI_BASE + "/dist/style.css"
    ] : process.env.KOISHI_ENV === "browser" ? [
      // @ts-ignore
      import_meta.url.replace(/\/src\/[^/]+$/, "/client/index.ts")
    ] : {
      dev: (0, import_path.resolve)(__dirname, "../client/index.ts"),
      prod: (0, import_path.resolve)(__dirname, "../dist")
    });
    const update = ctx.debounce(() => this.refresh(), 0);
    ctx.on("internal/fork", update);
    ctx.on("internal/runtime", update);
    ctx.on("internal/service", update);
    ctx.on("internal/status", update);
  }
  async get() {
    var _a, _b, _c;
    const nodes = [];
    const edges = [];
    const services = {};
    for (const [key, { type }] of Object.entries(this.ctx.root[import_koishi.Context.internal])) {
      if (type !== "service")
        continue;
      const instance = this.ctx.get(key);
      if (!(instance instanceof Object))
        continue;
      const ctx = (_a = Reflect.getOwnPropertyDescriptor(instance, import_koishi.Context.current)) == null ? void 0 : _a.value;
      if (ctx == null ? void 0 : ctx.scope.uid) {
        (services[_b = ctx.scope.uid] || (services[_b] = [])).push(key);
      }
    }
    for (const runtime of this.ctx.registry.values()) {
      let isActive = function(state) {
        return true;
      }, addNode = function(state) {
        const { uid, key, disposables, status } = state;
        const weight = disposables.length;
        const isGroup = name === "Group";
        const isRoot = uid === 0;
        const node = { uid, name, weight, status, isGroup, isRoot, services: services[uid] };
        if (key)
          node.name += ` [${key}]`;
        nodes.push(node);
      }, addEdge = function(type, source, target) {
        edges.push({ type, source, target });
      };
      __name(isActive, "isActive");
      __name(addNode, "addNode");
      __name(addEdge, "addEdge");
      const name = getName(runtime.plugin);
      const addDeps = /* @__PURE__ */ __name((state) => {
        var _a2;
        for (const name2 of runtime.using) {
          const instance = this.ctx.get(name2);
          if (!(instance instanceof Object))
            continue;
          const ctx = (_a2 = Reflect.getOwnPropertyDescriptor(instance, import_koishi.Context.current)) == null ? void 0 : _a2.value;
          const uid = ctx == null ? void 0 : ctx.state.uid;
          if (!uid)
            continue;
          addEdge("dashed", uid, state.uid);
        }
      }, "addDeps");
      const isReusable = (_c = runtime.plugin) == null ? void 0 : _c["reusable"];
      if (!isReusable) {
        if (!isActive(runtime))
          continue;
        addNode(runtime);
        addDeps(runtime);
      }
      for (const fork of runtime.children) {
        if (runtime.isForkable) {
          if (!isActive(fork))
            continue;
          addNode(fork);
          addEdge("solid", getSourceId(fork), fork.uid);
          if (!isReusable) {
            addEdge("solid", fork.uid, runtime.uid);
          } else {
            addDeps(fork);
          }
        } else {
          nodes[nodes.length - 1].weight += fork.disposables.length;
          addEdge("solid", getSourceId(fork), runtime.uid);
        }
      }
    }
    return { nodes, edges };
  }
};
__name(_Insight, "Insight");
var Insight = _Insight;
((Insight2) => {
  Insight2.Config = import_koishi.Schema.object({});
})(Insight || (Insight = {}));
var src_default = Insight;
//# sourceMappingURL=index.cjs.map
